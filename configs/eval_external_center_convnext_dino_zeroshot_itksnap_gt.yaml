project:
  name: external_center_convnext_dino_zeroshot_itksnap_gt
  seed: 42

runtime:
  device: cuda
  precision: amp
  num_workers: 4

data:
  root: dataset/external(center)/split_dataorigin
  image_dir: img
  mask_dir: mask_only_itksnap
  hrnet_result_dir: HRNet_Result
  image_extensions: [.jpg, .jpeg, .png, .bmp, .tif, .tiff]
  mask_extensions: [.nii.gz, .nii, .png, .jpg, .jpeg, .bmp, .tif, .tiff]
  result_extensions: [.png, .jpg, .jpeg, .bmp, .tif, .tiff]
  folds: [f1, f2]
  label_values: [0, 1, 2, 3]
  ignore_index: 255
  exclude_val_augmented: true

train:
  image_size: [768, 768]
  batch_size: 4
  amp: true
  freeze_backbone: false
  max_val_samples:

model:
  backbone: dinov3_convnext_tiny
  variant: tiny
  dinov3_code_dir: backbone/dinov3
  backbone_weights: weights/dinov3_convnext_tiny_pretrain_lvd1689m-21b726bb.pth
  decoder_channels: 192
  decoder_attention: none
  decoder_attention_reduction: 16
  decoder_deep_supervision: false
  num_outputs: 2
  head: conv

metric:
  threshold: 0.5
  tta:
    enabled: false
  postprocess:
    enabled: false
  intensity_refine:
    enabled: false
  fov_mask:
    enabled: false
  adaptive_threshold:
    enabled: false
  threshold_sweep:
    enabled: true
    thresholds: [0.05, 0.10, 0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.45, 0.50, 0.60, 0.70, 0.80, 0.90]
